{
  "name": "tryonar-marketing-website",
  "private": true,
  "version": "1.0.0",
  "description": "TryonAR Marketing Website Monorepo",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build",
    "dev:client": "cd apps/web && npm run dev",
    "dev:server": "cd apps/studio && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "dependencies": {
    "lucide-react": "^0.562.0"
  }
}
